plugins {
    alias(libs.plugins.kotlin.dsl)
}

repositories {
    mavenLocal()
    mavenCentral()

    google()
    gradlePluginPortal()
}

dependencies {
    api(libs.kotlin.plugin)
    api(libs.kotlin.serialization.json)
}
