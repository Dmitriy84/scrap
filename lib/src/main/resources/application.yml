app:
  vaultPrivateKeyPath: keys/auth-${spring.profiles.active}.key
  privateKeyID: auth-jwt-1
  url: https://gateway-web-markets-${spring.profiles.active}.tzero.com

enduser:
  password: Welcome@1

restassured:
  response:
    time: 10000
